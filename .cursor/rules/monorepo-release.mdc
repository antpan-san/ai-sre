---
description: 任意代码变更后自动执行发布 skill（勿等用户说「发布」）、K8s 离线回归（若适用）、push GitHub（ai-sre 同仓）
alwaysApply: true
---

# 发布后工作流（强制）

## 用户声明（代理须默认遵守）

- **每次**在本仓完成**与代码/配置/文档相关的变更**后，代理须**主动、完整**执行下文的发布流程（**Read** 并遵循 **`.cursor/skills/release-deploy/SKILL.md`** 及适用子 skill），**不得**等用户在对话里再说「发布 / 部署 / 上线 / ship / 跑 release」才执行。
- 仅当用户在本轮中**明确写出**豁免（例如「只改本地、不 SSH、不 push、不跑脚本」）时，才可跳过对应步骤，并须在回复中说明**跳过了什么**与**原因**。
- **对实验室 OpsFort 主机 `192.168.56.11` 的维护**（更新 **`/root/sre/bin/ai-sre`**、Nginx、后端、`verify-opsfleet-deployment` 所检项等）**一律**由代理在**本机**仓库根执行 **`./scripts/deploy-opsfleet-remote.sh`**、**`./scripts/deploy-remote.sh`** 及脚本内对远端的 **SSH** 自检完成；**用户无需、也不应**被要求**手动登录** 192.168.56.11 执行维护命令。未跑通适用发布步骤即宣称完成，视为违反本规则（用户豁免除外）。

工作区根目录：`/Users/panshuai/Documents/work/code/ai-sre`（**ai-sre** 与 **OpsFleetPilot** 同仓）。

在本仓库内对**任意文件**做出编辑、新增或删除后（含 `internal/`、`main.go`、`ft-*`、`deploy/`、`scripts/`、`README.md`、`docs/`、`.cursor/**` 等），在结束本轮任务前**必须**：

1. 使用 **Read** 打开 **`.cursor/skills/release-deploy/SKILL.md`**（发布部署总入口），再按其清单执行子 skill；其中 **CLI 同步与 push** 以 **`.cursor/skills/ai-sre-ship/SKILL.md`** 为准（README 核对、默认执行 `./scripts/deploy-remote.sh` 与远程冒烟、**推送到 GitHub** 等），除非用户在本轮对话中**明确**写出「不部署 / 不调远程 / 不改 README / 不 push」等豁免。
2. 若本次变更触及以下**任一**路径或主题，**另须**遵循 **`.cursor/skills/opsfleetpilot-ship/SKILL.md`**：`ft-backend/`、`ft-front/`、`deploy/`、`ansible-agent/`，或 `scripts/deploy-opsfleet-remote.sh`、`scripts/build-all.sh`、`scripts/verify-opsfleet-deployment.sh`、与 OpsFleet 部署相关的根 `README` 段落。**另**：凡修改 **`main.go`、根目录 `go.mod`、`go.sum`、`internal/cli/`（含版本号）或其它直接影响 **ai-sre 可执行文件** 的内容，**必须**在发布流程中执行 **`./scripts/deploy-opsfleet-remote.sh`**（以更新 **`$REMOTE_DIR/bin/ai-sre`** 及对外 **`GET .../cli/ai-sre`**）；**不得**仅以 **`deploy-remote.sh`** 代替，否则实验室 **install-ai-sre 仍发旧包**。
3. **Kubernetes 离线安装包 / 控制台 K8s 部署表单 / 内网制品镜像**相关变更时，**另须**遵循 **`.cursor/skills/k8s-offline-deploy-test/SKILL.md`**（人手等价、合并 inventory、测试与清理）。触发条件（命中**任一**即可）：`ansible-agent/`（尤其 `roles/`、`playbooks/`、`inventory/`）、`ft-backend/handlers/k8s_bundle.go`、`ft-backend/handlers/k8s_ansible.go`、`ft-backend/cmd/gen-k8s-bundle/`、`ft-front/src/views/service/k8s-deploy/`、`deploy/k8s-mirror/`、`ft-backend/handlers/k8s_mirror_catalog.go`、根目录 `install.sh` 或 `k8s_bundle` 相关逻辑。**最低限度**：`cd ft-backend && go build ./...`；`OPSFLEET_ANSIBLE_DIR=<repo>/ansible-agent go run ./cmd/gen-k8s-bundle ...` 成功产出 zip；若本机可 SSH 到测试控制机则按 Skill 做解压安装与反馈；**实验室 VM `root@192.168.56.11` 可达时**，按 Skill 核对制品 manifest 或部署脚本行为。**任一步失败则不得宣称完成且不 push**（除非用户豁免）。
4. **执行顺序**：先 **`ai-sre-ship`**（含 push 前检查）→ 若适用则 **`opsfleetpilot-ship`** → 若适用则 **`k8s-offline-deploy-test`** → 最后 **`git add/commit/push`**（以 `ai-sre-ship` 第 5 步为准，勿在未跑完适用 skill 时 push）。  
5. **ai-sre 在 OpsFort 上的可下载版本**：`install-ai-sre.sh` / `GET .../cli/ai-sre` **只读** `ft-backend` 所配置的 `opsfleet.ai_sre_binary_path`（全栈部署时一般为 **`$REMOTE_DIR/bin/ai-sre`**，由 `build-all.sh` 生成）。**仅**执行 `deploy-remote.sh` 会在远端的 **`$REMOTE_DIR/ai-sre`（仓库根下单独 go build）** 产物，**不会**自动覆盖 `bin/ai-sre`，因此控制台对外的 ai-sre **可能仍是旧版**。**每次发布**若改了 `ai-sre` 或提升 `internal/cli` 版本，须在 OpsFort 侧核对：`ssh` 上 `$OPSFLEET_AISRE_BINARY_PATH` 的 `./ai-sre version` 与 **`curl .../ft-api/api/k8s/deploy/cli/ai-sre/version`** 的 JSON 是否与本仓一致；不一致则**必须**重跑 **`deploy-opsfleet-remote.sh`**（或手动将新二进制拷到 `bin/ai-sre` 后 `systemctl restart opsfleet-backend`）。详见 **`.cursor/skills/opsfleetpilot-ship/SKILL.md`「ai-sre 分发与版本」** 与 **`.cursor/skills/release-deploy/SKILL.md`** 检查项。

不得在未执行对应 skill 要求的情况下宣称发布完成或催促用户直接 `git push`。
