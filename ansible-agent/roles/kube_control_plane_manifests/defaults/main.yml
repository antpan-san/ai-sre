---
# 控制台合并 inventory 时会覆盖；仅供裸 ansible-agent 调试
kube_apiserver_image: "registry.k8s.io/kube-apiserver:{{ kubernetes_version }}"
kube_controller_manager_image: "registry.k8s.io/kube-controller-manager:{{ kubernetes_version }}"
kube_scheduler_image: "registry.k8s.io/kube-scheduler:{{ kubernetes_version }}"
